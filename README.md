# AI
**Кейс для Т-Образования.**\
*Задача*: Создать REST API сервис, который сможет обнаруживать логотип Т-Банка (игнорируя логотипы "Тинькофф") на загружаемых изображениях и возвращать координаты найденных логотипов.

# Оглавление:
1. [План работ](#План-работ)
2. [Технологический стек](#технологический-стек)
3. [Ход выполения](#ход-выполения)
4. [Инструкции по установке](#инструкции-по-установке)
5. [Документация решения](#документация-решения)

<br><br/>
---
<br><br/>

# План работ:
В этом разделе вы найдёте задачи, которые были выполнены в рамках проекта и те, что ещё предстоит выполнить.
- [ ] Подготовка данных и разметка (*04-10.09.2025*)
  - [x] Установка зависимостей (*04.09.2025*)
  - [x] Проверка совместимости компонетов (*04.09.2025*)
  - [x] Создайте Dockerfile
  - [x] Разработка предварительной архитектуры (*05.09.2025*)
  - [x] Дополнение датасета
  - [x] Разметка датасета Fine-tuning подход (*11-14.09.2025*)
  - [x] Разделение на train/val и финализация (*14.09.2025*)
  - [x] Проверка (*14.09.2025*)

- [x] Обучение модели (*14.09.2025*)
     
- [ ] Реализация API сервиса (*16.09.2025*)
  - [ ] Создание FastAPI-приложения с эндпоинтом /detect: загрузка файла, обработка моделью, возврат DetectionResponse.(*16.09.2025*)
  - [ ] Тестирование и отладка (*16.09.2025*)
     
- [x] Валидация и метрики (*14-16.09.2025*)
  - [x] Написать Python-скрипт для подсчета Precision/Recall/F1 на val-наборе.
  - [x] Использовать OpenCV для отрисовки bounding boxes на изображениях.
     
- [ ] Доработка
  - [ ] Форматирование проверка и дополнение документации

<br><br/>
---
<br><br/>

# Технологический стек
Данный раздел описывает оптимальный технологический стек для реализации проекта детекции логотипа Т-Банка на основе машинного обучения и REST API. Стек выбран с учетом требований кейса (производительность на GPU T4, ограничения по времени ≤10 с, поддержка форматов изображений, интеграция zero-shot и Fine-tuning подходов) и известных трендов разработки (на момент составления).

| Категория |	Компонент	| Версия | Обоснование и цель |
| --- | --- | --- | --- |
| Язык программирования	| Python	| 3.13	| Базовый язык для всего проекта. Python 3.13 предлагает улучшенную производительность в async/await (лучше чем 3.12), что идеально для FastAPI-инференса. Использование cpython 3.13.0+ для оптимизаций в ML-задачах. |
| Фреймворк API |	FastAPI + Pydantic |	FastAPI 0.115+, Pydantic 2.10+ |	Для строгого REST API с автогенерацией документации (Swagger). Pydantic обеспечивает валидацию входных данных (размеры изображений, форматы). Поддерживает async для параллельной обработки изображений, укладываясь в 10-сек лимит. |
| Сервер ASGI |	Uvicorn |	0.31+ |	Запуск FastAPI в продакшн. Легкий, быстрый, с поддержкой нескольких GPU. |
| ML-фреймворк |	PyTorch	| 2.4+	| Основной для нейронных сетей (YOLO, CLIP). Версии 2.4+ имеют улучшенный инференс с TorchCompile JIT.
| Модели ML |	YOLO11 (ultralytics) |	8.3+	| Для детекции объектов (*bounding boxes логотипа*). YOLO11 быстрее и точнее предшественников, а так же хорошо подходит для доочучения. |
| Zero-shot ML |	CLIP (OpenAI, via transformers) |	Hugging Face transformers 4.46+ |	Для zero-shot классификации логотипов без полного датасета. Интегрируется с PyTorch; использование model "openai/clip-vit-base-patch16" для speed/accuracy. |
| Обработка изображений |	OpenCV + Pillow	| OpenCV 4.10+, Pillow 11.0+ |	OpenCV для предобработки (*grayscale, resize*), Pillow для загрузки/сохранения внешних форматов (JPEG, PNG, BMP, WEBP). Дополняют друг друга без конфликтов. |
| HTTP-клиент	| Requests	| 2.32+	| Для загрузки изображений по URL (асинхронно в FastAPI). Простой и надежный. |
| Контейнеризация |	Docker	| 27+	| Для iso-пакетарования приложения. **Добавить multi-stage builds для уменьшения размера образа**. |
| GPU-ускорение	| CUDA Toolkit	| 12.6+ |	Для PyTorch GPU. **На T4: установить drivers NVIDIA 470+**. |
| Тестирование и валидация	| Pytest + Sklearn + TorchEval	| Pytest 8.3+, Sklearn 1.5+, TorchEval 1.2+	| Pytest для unit/integration tests. Sklearn/TorchEval для ML-метрик (F1-score, Precision/Recall при IoU=0.5). ***Убедитться, что модель достигает 25% точности на тестовом сете***. |
| Логирование и мониторинг	| Loguru + prometheus-fastapi-instrumentator	| Loguru 0.7+, prometheus-fastapi 2.0+	| Loguru для структурированных логов (ошибки, timeouts). Prometheus для метрик internal (inference time, GPU usage) — **интегрировать с Grafana**. |
| Дополнительные утилиты	| NumPy, Matplotlib	| NumPy 2.1+, Matplotlib 3.9+	| NumPy для векторных операций. Matplotlib для визуализации результатов (bounding boxes на изображениях) в документах/отчетах. |

<br><br/>
---
<br><br/>

# Ход выполения
В этом разделе зафиксированы все шаги, сделанные в рамках этого кейса.
1. Был составлен примерный план работ и первичная документация на основе: <ins>анализа задачи</ins>, <ins>поиска аутуальных технологий</ins> (*открытые источники и нейросети*) и <ins>тайм менеджмента</ins>. (*2 час*)
2. В рамках этапа *установка зависимостей* были проделаны следующие шаги: (*1,5 часа*)
  _ Для повышения удобства использования были созданы .bat файлы, автоматизирующие установку;
  _ Протестированы зависимости;
  _ Обновлена документация.
3. Создана примерная структура проекта. Подключен и проверен докер. (*0,5 часа*)
4. В связи с болезнью и большим количеством неотложных дел (управление проектами, составление программы для Движения первых, проведение лекций) решение кейса было отложено до 11 числа.
5. Был добавлен простой gui для запуска компонентов проекта.
6. Первым делом я написал простой скрипт для разметки и запустил его. Дождавшись результатов, я получил не очень качественное разбиение на позитивные и негативные примеры, из-за чего я обратился к интернету в поисках более эффективных способов решения данной задачи.
7. Для повышения эффективности тестирования я уменьшил выборку и добавил копирование позитивных результатов в отдельную папку. Также я проверил YOLOv10, YOLOv11 вместо 8 версии и попробовал более большую версию для сравнения. Результат получился точнее, но всё ещё далёк от цели. Следующим шагом проверю 9 версию, так как пользователи пишут, что она точнее в некоторых задачах и поиграюсь с позитивными и негативными промптами.
8. Я попробовал разные модели и разные промпты, но результат меня по прежнему не устраивает, по этому я решил попробовать Fine-tuning подход, посмотрим, что получится. Мои шаги:
  _ Первым делом был установлен и запущен через Докер CVAT для удобной ручной разметки;
  _ На скорую руку были выбраны 104 наиболее интересных изображения с логотипами Т-Банка;
  _ На разметку тренировочного датасета ушёл примерно час + 15 минут на проверку и уточнение рамок. Для разметки я проанализировал свойства логотипа и выделил 3 основных: геометрическая форма щита, геометрическая форма буквы Т в щите, контекст окружения. Возможно нейросеть обращает внимание на другие признаки, но последующие эксперименты более-менее подтвердили мою теорию, что для уверенного определения логотипа необходимо, чтоб сочетались не менее 2 признаков: геометрическая форма щита (достаточно нижней части) + любуй другой признак. Да, это не совсем честное и универсальное решение, но учитывая контекст задаччи и ситуации, в которых логотип Т-Банка встречается, то я считаю это приемлимым допущением;
  _ На размеченном датасете дообучена модель YOLOv11m со следующими результатами (НО на пути к этому пришлось столкнуться с проблемой совместимости библиотек и драйверов видеокарты, дважды поймать синий экран и ещё немного приключений. С проблемой удалось разобраться и, вроде бы, даже достаточно универсально):
    **100** epochs completed in 0.693 hours: Обучение заняло всего ~41 минуту. Это быстро.
    YOLO11m summary ... 20,030,803 parameters: Я использовал модель "medium" размера, что является хорошим балансом между скоростью и точностью.
    all **34 31 0.997 0.968 0.977 0.907**: Это ключевая строка! Валидация на 34 изображениях с 31 размеченным логотипом:
      * Box(P) = **0.997 (Precision)**: Почти идеальная точность. Это значит, что 99.7% из всех обнаруженных моделью bounding box'ов являются настоящими логотипами. Очень низкий уровень ложных срабатываний (False Positives).
      * Box(R) = **0.968 (Recall)**: Отличная полнота. Это значит, что модель нашла 96.8% всех логотипов, которые были на изображениях. Очень мало пропусков (False Negatives).
      * mAP50 = **0.977 (mean Average Precision at IoU=0.5)**: Это главная метрика для детекции. Значение 0.977 (из 1.0) - это фантастический результат. Он объединяет в себе и Precision, и Recall.
      * mAP50-95 = **0.907 (mAP @ IoU from 0.5 to 0.95)**: Эта метрика гораздо строже, она требует, чтобы bounding box'ы были очень точно нарисованы. Значение 0.907 также является исключительно высоким.

    Вывод: модель обучилась великолепно. Она обладает высочайшей точностью и почти идеальной полнотой. **Вот только велик шанс переобучения**, так как такие результаты выглядят не реалистично, по этому я зациклил систему: 
      * обучаю модель на подготовленной выборке;
      * обученной моделью классифицирую новые изображения;
      * Визуально проверяю часть положительных результатов, удаляю ложные срабатывания, корректирую bounding box'ы (если есть время);
      * Обучаю модель на полученной выборке;
      * Повторяю.
    Таким образом через несколько циклов удалось уточнить результат обучения до:
      * Ultralytics 8.3.28 ЁЯЪА Python-3.13.5 torch-2.8.0+cu129 CUDA:0 (NVIDIA GeForce RTX 4060 Laptop GPU, 8188MiB)
      * YOLO11m summary (fused): 303 layers, 20,030,803 parameters, 0 gradients, 67.6 GFLOPs
      === Validation Results ===
      Precision (mp): **0.9968108557168683**
      Recall (mr): **0.9259259259259259**
      mAP50: **0.9347927461139897**
      mAP50-95: **0.8393495117181662**
      F1-score: **0.960**
      Raw Precision list (per class): [    **0.99681**]
      Raw Recall list (per class): [    **0.92593**]

      * all        166         54      0.997      0.926      0.935      0.839
      * Speed: **0.6ms preprocess**, **41.5ms inference**, **0.0ms loss**, **1.2ms postprocess** per image

    Правда здесь оказался подводный камень: YOLO всегда пытается запуститься на CPU, если иное не указано в настройках. Возможно это связано с моей машиной?
9. Несмотря на то, что я вижу явные недостатки данного подхода, я решил остановиться на этом. Дальнейшее переобучение модели результат сильно не изменит, но займёт много времени, а точность уже достаточна для выполнения задания. Так как, в идеале, нужно заложить время на доведение до ума вспомогательной архитектуры (батники запуска, gui для управления всеми компонентами проекта, автоматизация настройки и установки под разные системы и тд), я приступил к созданию FastAPI приложения.
10. Тест API показал следующие результаты:
  Total requests: 50
  Successful: 50
  Failed: 0

  Performance metrics:
  Total time: 9788.40 ms (9.79 s)
  Average response time: 70.02 ms
  Min response time: 15.85 ms
  Max response time: 283.79 ms
  Total detections: 11
  Requests per second: 5.11

  Performance by format:
  .jpg: 45 requests, avg: 69.40 ms, avg detections: 0.2
  .png: 5 requests, avg: 75.59 ms, avg detections: 0.2
  ✅ SUCCESS: All responses under 10 seconds (max: 0.28s)
  Results saved to load_test_results.json
11. Оставшееся время я потратил на дообучение модели на более сложных ситуациях.


<br><br/>
---
<br><br/>

# Инструкции по установке
В этом разделе указаны инструкции по установке проекта и его развёртыванию.<br><br/>
Если вы скачали и распоковали репозиторий, то для полной установки проекта вам осталось:
1. Скачайте данные по ссылке: https://drive.google.com/drive/folders/1minFkpicDwTBee0yWc74N8mzEz4iXYsC?usp=sharing
  1.1. Переместите ***yolo11m_tbank_best.pt*** в ***"data\models"***;
  1.2. Переместите <ins>**содержимое**</ins> папки ***val*** в ***data\input***;
  1.3. Переместите <ins>папку</ins> ***test*** в ***data\input***;
  1.4. (Опционально) Загрузить не размеченный датасет в папку ***data\input\raw***.
2. Варианты запуска:
  1) Запустить файл ***start_project.bat***, следуйте инструкциям, дождитесь загрузки и нажать *Enter*. (*Проверит и установит зависимости, запустит интерфейс управления всем проектом*)
  2) Запустить файл ***gui.py***. (*Прямой запуск интерфейса*) (*что бы GUI работал нормально, нужно включить в системе кодировку utf-8*<ins>***сделать сноску***</ins>)
  3) Открыть корневую папку проекта в терминале, ввести команду: ***run_docker.bat***, нажать *Enter*. (Запустит докер API, предварительно проверив доступность GPU и выбрав настройки подходящие системе)
  4) Открыть корневую папку проекта в терминале, ввести команду: ***docker run -p 8080:8080 --gpus all t-bank-logo***, нажать *Enter*. (*Прямой запуск API через докер с принудительным использованием GPU*)
Приятного использования ;)

Для пересборки докера необходимо:
1. Установить **_python 3.13.*_** и ***Docker Desctop***;
2. Убедиться, что у вас стабильное подключение к интернету;
3. Запустить файл ***build_docker.bat*** и дождаться завершения его выполнения (*может занять от 5 до 35 минут*).

**Для корректной работы приложения порт 8080 должен быть свободен!**
<br><br/>

<br><br/>
---
<br><br/>

# Документация решения
Этот раздел посвящён детальному описанию решения.
<br><br/>
* В целях улучшения пользовательского опыта сборка и запуск решения полностью перенесены в исполняемые файлы (*.bat выбраны за простоту их создания и достаточность интерфейса для этих задач*).
* Все коммиты большие и льются в мэйн, так как проект не подразумевает коллективной разработки, а время выполнения кейса строго ограничено.

## Структура проекта
project-root/
├── data/                    # Данные (input/output)
│   ├── input/              # Исходные данные (изображения для YOLO)
│   └── models/             # Обученные модели (на основе yolov11.pt)
├── src/                    # Основной код (разбито по модулям)
│   ├── __init_._.py         # Пустой файл для Python-пакета
│   ├── main.py             # Точка входа: FastAPI app + инициализация
│   ├── api/                # API-эндпоинты
│   │   ├── __init_._.py
│   │   ├── routes.py       # Маршруты (POST /detect, GET /health)
│   │   └── depends.py      # Зависимости
│   ├── services/           # Бизнес-логика (обработка запросов)
│   │   ├── __init_._.py
│   │   ├── ml_service.py   # Инференс YOLO: загрузка модели, предсказания
│   │   └── image_service.py # Обработка изображений (resize, preprocess)
│   └── models/             # Используемые модели/Pydantic
│       ├── __init_._.py
│       └── schemas.py      # Схемы запросов/ответов (ImageRequest, DetectionResponse)
├── configs/               # Конфигурации (заменяют хардкод)
│   ├── __init_._.py
│   └── config.yaml        # Параметры (порт, модель_path, GPU флаги)
├── tests/                 # Тесты (pytest для микротестов)
│   ├── __init_._.py
│   ├── test_api.py      # Тесты эндпоинтов
│   └── test_ml.py       # Тесты ML-службы
├── scripts/              # Скрипты (запросы, предобработка)
|   ├── __init_._.py
│   ├── prepare_dataset.py # Скрипт zero-shot разметки
│   ├── fine_tune_yolo.py  # Скрипт Fine-tuning подхода
│   └── visualize_positive.py # Визуализирует bounding box'ы на не более чем 100 "позитивных" изображений из папки ***data\input\positive***
├── Dockerfile           # Контейнер
├── requirements.txt     # Зависимости (fastapi, ultralytics, opencv, ...)
├── build_docker.bat     # Файл запуска сборки докера
├── run_docker.bat       # Файл запуска докера
├── gui.py               # Gui проекта
├── start_project.bat    # Файл установки зависимостей и запуска gui.py
├── README.md
└── .gitignore

<br><br/>

Система разбита на компоненты, соответствующие этапам решения кейса:
1. Разметка датасета и обучение модели
2. FastAPI app
3. Тестирование модели

## Разметка датасета и обучение модели
Для этого создан файл ***scripts/fine_tune_yolo.py*** который разбивает содержимое папки *'data/input/positive'* в форматах *'.jpg'*, *'.jpeg'*, *'.png'*, *'.bmp'* на обучающую и валидационную выборки, дообучает модель **yolov11m.pt**, проводит тестирование и сохранение модели.
Далее файл ***scripts/prepare_dataset.py*** берёт содержимое папки 'data/input/raw' и через обученную на прошлом шаге модель находит все подходящие прямоугольные области на изображении и записывает результат в YOLO формате в файлы по пути *'data/input/positive'*.

Таким образом, используя файл визуализации ***visualize_positive.py***, можно быстро классифицировать новые выборки, находить ошибки, исправлять их в **CVAT** (*устанавливается отдельно*) и переобучать модель.
<br><br/>

## FastAPI app
...
<br><br/>

## Тестирование модели
...
